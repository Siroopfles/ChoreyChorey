// This file is a duplicate and is intentionally left empty to resolve a build conflict.
// The correct Tailwind configuration is located at the project root.
const config = {};
export default config;
